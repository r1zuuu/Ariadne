node_modules/
.env
# The backend address is baked in at build time, so this file differs per
# machine: the laptop points at localhost, the release build at Render.
.env*.local
dist/
setup.md

# Wyniki pomiaru wyszukiwania: generowane przez scripts/eval-*.ts.
# Zrodlem zestawu pytan jest scripts/eval-questions.ts, nie ten katalog.
backend/eval/
